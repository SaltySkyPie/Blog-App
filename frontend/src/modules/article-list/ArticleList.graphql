


query getArticles($limit: Int, $offset: Int) {
  articles(limit: $limit, offset: $offset) {
    id
    title
    perex
    createdAt
    imageUrl
    user {
      id
      firstName
      lastName
      middleName
    }
  }
}